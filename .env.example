# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Server
PORT=3001
NODE_ENV=development

# JWT (from Supabase Dashboard > Settings > API > JWT Secret)
JWT_SECRET=your-jwt-secret

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# App URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# ============================================================================
# SUPABASE AUTH SETUP (OAuth)
# ============================================================================
# For OAuth (Google, GitHub, etc.) to work, you must add redirect URLs
# in your Supabase Dashboard:
#
# 1. Go to: Supabase Dashboard > Authentication > URL Configuration
# 2. Add these to "Redirect URLs":
#    - http://localhost:3000 (frontend - for client-side auth)
#    - http://localhost:3001/api/auth/callback (backend - for server-side OAuth)
#
# For production, add your production URLs:
#    - https://your-frontend.com
#    - https://your-backend.com/api/auth/callback
#
# ============================================================================
